//* Paths for Client
const clientRoutes = {
    home: '/',
}

export const { home } = clientRoutes;
//* End of paths for Client

//* Paths for Admin
const adminRoutes = {
    dashboard: '/admin/',
}

export const { dashboard,  } = adminRoutes;
//* End of paths for Auth

//* Auth
const authRoutes = {
    login: '/login',
    register: '/register',
}

export const { login, register } = authRoutes;
//* End of paths for Auth